"use client";

import PublicStoresListWidget
  from '@/app/[locale]/(public)/stores/(widgets)/PublicStoresListWidget';

export default function PublicStoresListPage() {
  return <PublicStoresListWidget />;
}
