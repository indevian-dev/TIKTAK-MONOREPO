import { PublicTermsWidget }
    from '@/app/[locale]/(public)/docs/terms/(widgets)/PublicTerms.widget'

export default function PublicTermsPage() {

    return (
        <>
            <PublicTermsWidget />
        </>

    )
}