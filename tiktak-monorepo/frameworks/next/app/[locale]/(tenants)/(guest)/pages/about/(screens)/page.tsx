import { PublicAboutWidget }
    from '@/app/[locale]/(tenants)/(guest)/pages/about/(widgets)/PublicAboutWidget'

export default function PublicAboutPage() {

    return (
        <>
            <PublicAboutWidget />
        </>

    )
}