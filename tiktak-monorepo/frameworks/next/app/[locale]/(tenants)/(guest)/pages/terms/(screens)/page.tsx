import { PublicTermsWidget }
    from '@/app/[locale]/(tenants)/(guest)/pages/terms/(widgets)/PublicTermsWidget'

export default function PublicTermsPage() {

    return (
        <>
            <PublicTermsWidget />
        </>

    )
}