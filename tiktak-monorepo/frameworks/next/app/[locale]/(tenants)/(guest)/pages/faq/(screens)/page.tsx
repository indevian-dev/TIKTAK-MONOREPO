import { PublicFaqWidget } from '@/app/[locale]/(tenants)/(guest)/pages/faq/(widgets)/PublicFaqWidget'

export default function PublicFaqPage() {

    return (
        <>
            <PublicFaqWidget />
        </>
    )
}