
import PublicAccountWidget
  from '@/app/[locale]/(tenants)/(guest)/accounts/(widgets)/PublicAccountWidget';

function PublicAccountScreen() {

  return (
    <PublicAccountWidget />
  );
}

export default PublicAccountScreen;
